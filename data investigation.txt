data<-read.csv('test.csv', header = TRUE)

nrow(data)
ncol(data)

unique_data<-matrix(0, nrow = 100, ncol = ncol(data))

for (n in 2:(ncol(data)-14)){
unique_data[(1:length(unique(data[,n]))),(n-1)]<-levels(unique(data[,n]))
}

length(unique(data[,2]))
